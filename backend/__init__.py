"""RemoveBG Python backend package.

This marker is required when the service is bundled with PyInstaller: the
entry script imports modules as ``backend.*`` and therefore the complete
package must be collected into the frozen executable.
"""
