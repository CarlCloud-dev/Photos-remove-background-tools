# RMBG-2.0 / BiRefNet backend requirements (flexible pinned for Python 3.10..3.13)
# ------------------------------------------------------------------
# PyTorch 2.9.x can import in the development interpreter but fails during
# initialization inside this PyInstaller bundle.  2.6.0 / 0.21.0 is the first
# CPU-compatible pair with Python 3.13 wheels and remains compatible with the
# supported Python 3.10--3.13 build matrix.
# ------------------------------------------------------------------
# 发布版固定使用 CPU PyTorch：模型按需下载，CUDA 运行库则不应被打进每
# 个用户的安装包。国内 CPU 轮子优先，官方 CPU 源只作为镜像缺失时的回退。
--index-url https://pypi.tuna.tsinghua.edu.cn/simple
--find-links https://mirrors.aliyun.com/pytorch-wheels/cpu/
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0+cpu
torchvision==0.21.0+cpu
transformers>=4.44.2,<4.47
timm==1.0.19
einops>=0.8.0,<0.9
# InSPyReNet 官方透明背景实现；应用直接从统一模型缓存加载权重，禁止其内置下载器写入用户目录。
transparent-background==1.3.4
pillow>=10.4.0,<12
kornia>=0.7.3,<0.9
# 内置 Alpha Matting 精修：开发环境和 PyInstaller 包均包含，不在用户运行时下载。
pymatting>=1.1.12,<2
scipy>=1.10,<2
numba>=0.61,<0.64
flask>=3.0.3,<3.1
waitress>=3.0.0,<4
huggingface_hub>=0.25.0,<0.28
pyinstaller>=6.10.0,<7
